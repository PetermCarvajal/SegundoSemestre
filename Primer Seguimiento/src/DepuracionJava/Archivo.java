package DepuracionJava;

public class Archivo {
    public static void main(String[] args) {
        int c=100;
        int d=114;
        int sum=suma(c,d);

        System.out.println("El Area de Su estructura de minecraft con unas Medidas de "+c+" y "+d+" es "+sum);
    }
    public static int suma(int x, int y) {
        int resultado;

        int Multiplicador=214;
        resultado=214*Multiplicador;

        return(resultado);
    }
}
